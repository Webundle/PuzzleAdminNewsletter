{% trans_default_domain 'admin' %}

<div class="modal-dialog">
    <div class="modal-content">
        <div class="modal-header bg-info">
            <h3>{{ "newsletter.group.update"|trans }} {{ group.name|capitalize }}</h3>
        </div>
        {% include 'PuzzleAdminNewsletterBundle:Group:form.html.twig' with { 'form' : form, 'group' : group } %}
    </div>
</div>
<script>
    // Default initialization
    $('.select').select2({
        minimumResultsForSearch: Infinity
    });
</script>